// Merge this into your active OpenClaw configuration.
// The OpenAI-compatible Chat Completions endpoint is disabled by default.
{
  gateway: {
    http: {
      endpoints: {
        chatCompletions: {
          enabled: true
        }
      }
    }
  }
}
